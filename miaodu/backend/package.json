{
  "name": "miaodu-backend",
  "version": "1.0.0",
  "description": "妙读电子书拆解平台后端",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute zhihr_db --file=./migrations/000_create_tables.sql",
    "db:migrate:remote": "wrangler d1 execute zhihr_db --file=./migrations/000_create_tables.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241201.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^4.6.12"
  }
}
