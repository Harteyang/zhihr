{
  "name": "zhihr-api",
  "version": "1.0.0",
  "description": "知HR工具集后端API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node tests/talent_parsers.test.js && node tests/parse_file.test.js && node tests/fix_verification.test.js",
    "db:init": "wrangler d1 execute zhihr_db --file=./schema.sql",
    "db:migrate-talent": "wrangler d1 execute zhihr_db --file=./migrations/001_talent_pool_users.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "unpdf": "^0.12.0",
    "fflate": "^0.8.0"
  }
}
