# Quick Task 260522-n9w: 移动端记录页折叠/展开功能

**Date:** 2026-05-22
**Status:** Completed

## Description

为复盘系统"记录"页面的所有填写项，在移动端（< 768px）添加折叠和展开功能。PC Web 端保持不变。

## Tasks

### Task 1: mC 组件添加折叠/展开支持

**Files:** `review-system/assets/index-BmCB11Ko.js`

**Action:**
- 为 `mC` 组件添加 `collapsed` 和 `onToggle` props
- 当 `onToggle` 存在且 `collapsed` 为 true 时，隐藏 children 内容
- 当 `onToggle` 存在时，显示 "▶ 展开" / "▼ 收起" 切换按钮

**Verify:** PC端调用不传 `onToggle`，无任何 UI 变化 ✓

### Task 2: cG 组件添加折叠/展开支持

**Files:** `review-system/assets/index-BmCB11Ko.js`

**Action:**
- 为 `cG`（今日总结与反思）组件添加 `collapsed` 和 `onToggle` props
- 当 `onToggle` 存在且 `collapsed` 为 true 时，隐藏 textarea 内容
- 当 `onToggle` 存在时，显示切换按钮

**Verify:** PC端调用不传 `onToggle`，无任何 UI 变化 ✓

### Task 3: xC 组件添加移动端检测和状态管理

**Files:** `review-system/assets/index-BmCB11Ko.js`

**Action:**
- 添加移动端检测：`window.innerWidth < 768`，监听 resize 事件
- 添加折叠状态管理：`useState({})` 追踪每个维度的折叠状态
- 仅为移动端传递 `onToggle`：`onToggle: p ? () => b(h.key) : void 0`
- 所有 8 个维度（健康、工作、学习、社交、财务、生活、精神、休闲）均支持折叠
- "今日总结与反思"也支持折叠

**Verify:**
- PC端（≥768px）：无折叠按钮，所有内容正常展开 ✓
- 移动端（<768px）：每个卡片显示 "▼ 收起"，点击后折叠为 "▶ 展开" ✓
- TG 历史记录组件不受影响 ✓